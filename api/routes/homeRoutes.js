const express = require('express');
const cors = require('cors');
const router = express.Router();
const jwt = require('jsonwebtoken')

router.put('/Home/AccountEdit', async(req, res) => {
	
})


module.exports = router;
